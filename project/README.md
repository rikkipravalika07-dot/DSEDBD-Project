# Expense Splitter Frontend

React.js + Vite + HTML5 + CSS3 + JavaScript ES6.

The frontend calls the Node.js/Express API at `http://localhost:5000/api` by default.

## Run
npm install
npm run dev

If your backend uses another URL, create `.env` from `.env.example` and set `VITE_API_URL`.
